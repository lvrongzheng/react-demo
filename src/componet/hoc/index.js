/**
 * 高阶组件（higher-order component），类似于高阶函数，它接受 React 组件作为输入，输出一
   个新的 React 组件。
   实现高阶组件的方法有如下两种。
     属性代理（props proxy）。高阶组件通过被包裹的 React 组件来操作 props。 
     反向继承（inheritance inversion）。高阶组件继承于被包裹的 React 组件。
 */
